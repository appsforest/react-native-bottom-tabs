---
'@appsforest/react-native-bottom-tabs': minor
'@appsforest/react-navigation': minor
---

Sync with upstream. `tabBarIconRenderingMode` now accepts `'automatic' | 'original'`: replace `'alwaysOriginal'` with `'original'`, `'alwaysTemplate'` was removed.
